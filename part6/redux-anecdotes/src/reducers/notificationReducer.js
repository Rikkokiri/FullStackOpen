const initialState = {
  notification: 'initial notification value',
}

const notificationReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default notificationReducer
